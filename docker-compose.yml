version: "3.8"

services:
  portfolio-site:
    image: portfolio
    container_name: portfolio-site
    restart: always
    ports:
      - 3000:3000
    build:
      context: .
      dockerfile: dockerfile
    environment:
      NODE_ENV: production
      
networks:
  default:
    name: proxy-network